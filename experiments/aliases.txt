# Format:
#   [\s] alias \s name [\s] [# comment]
# where \s means whitespace
# and   [] means optional
# empty lines are allowed

github.com/ledgerwatch/erigon/core/vm.run  github.com/ledgerwatch/erigon/core/vm.(*EVMInterpreter).Run  # Shouldn't this be optimized out ? it's also tail call
